CC='../sampler-1.6.2/driver/sampler-cc-here' CFLAGS='-fsampler-random=online -fsampler-scheme=branches -fsampler-scheme=returns -fsampler-scheme=scalar-pairs' LDFLAGS='-fsampler-random=online -fsampler-scheme=branches -fsampler-scheme=returns -fsampler-scheme=scalar-pairs' ./configure 
